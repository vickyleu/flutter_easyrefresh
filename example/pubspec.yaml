name: example
description: EasyRefresh example.
version: 2.0.4+32

environment:
  sdk: ">=2.6.0-dev <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  cupertino_icons: ^0.1.2
  url_launcher: ^5.2.5
  flutter_swiper: ^1.1.6
  flutter_spinkit: ^4.0.0
  flare_flutter: ^1.6.4
  flutter_i18n: ^0.8.0
  extended_nested_scroll_view: ^0.3.8
  flutter_easyrefresh:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter


flutter:
  uses-material-design: true
  assets:
    - assets/image/
    - assets/locale/
    - assets/flare/